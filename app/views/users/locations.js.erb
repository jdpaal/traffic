<% @locations.each do |location| %>

  var
    lat = "<%= location.lat %>",
    lng = "<%= location.lng %>",
    googleLatLng = new google.maps.LatLng(lat, lng);

  placeMarker(googleLatLng);

<% end %>